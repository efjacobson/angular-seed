(function() {
	'use strict';

	describe('myApp.panel2 module', function() {

	  beforeEach(module('myApp.panel2'));

	  describe('panel2 controller', function(){

	    it('should ....', inject(function($controller) {
	      //spec body
	      var panel2Controller = $controller('Panel2Controller');
	      expect(panel2Controller).toBeDefined();
	    }));

	  });
	});
})();